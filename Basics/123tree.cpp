#include <bits/stdc++.h>

using namespace std;

int floor_log_2(int x){
    for(int i = 0; )
} 


void solve() {
	int a, b, c;
	cin >> a >> b >> c;
	if (c != a + 1) {cout << -1 << '\n'; return;}
	if (a + b + c == 1) {cout << 0 << '\n'; return;}

}

int main() {
	int tt; cin >> tt; for (int i = 1; i <= tt; i++) {solve();}
	// solve();
}